// primitive
// stored directly in variable
// stored on the stack (actual values)

// strings
const name = 'John';
let hacker = 'Ezzi';

// numbers
const no = 46;

// boolean
const hasKids = true;

// null
const people = null;

// undefined
let test;

// symbol
const sym = Symbol();

// Refference type
// access by refferance
// store in a heap(dynamic memory)

// arrays
const car = ['honda', 'toyota', 'nissan'];

// object litterals
const address = {
    city: 'Boston',
    state: 'MA'
}

// date
const today = new Date();

// how to see the data type
console.log(typeof address);


